{
  "name": "blackjack-master",
  "version": "1.0.0",
  "description": "BLACKJACK MASTER — אפליקציית לימוד ותרגול בלאק ג'ק בעברית",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "node --test --test-timeout=600000 tests/*.test.ts",
    "serve": "npx --yes serve web -l 4173",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "engines": {
    "node": ">=22.6"
  }
}
